# P1（宿主 root）最高权限取证记录 · 归档

| 项目 | 内容 |
|---|---|
| 归档日期 | 2026-09-23（自 v1.1 报告提取归档） |
| 层级 | **P1** —— 宿主 `uid=0(root)`，经 `su -c`（Magisk Alpha） |
| 载体 | Redmi K30 5G（`picasso`），LineageOS 22.2 UNOFFICIAL，Magisk Alpha v`c3db2e36-alpha` |
| 用途 | 本机当前主载体无 P1；**本归档保存早期在带 root 对照载体上取得的全部 P1 观测，不因载体不在场而删除** |
| 关联 | [`README.md`](README.md) §2.2 / §4.2 / §4.4 / §4.9 / §4.13 |

> **说明**：P1 层级可读取 P0/P2 均不可达的对象：
> `/proc/<pid>/{ns,maps,fd,environ}`、`/data/data/<pkg>/`、`/data/app/*/<pkg>*/lib/arm64/`。
> 以下记录均为当时的真实终端回显。

---

## 1. 命名空间：无内核级隔离（P1 / K1）

```text
$ su -c 'ls /proc/<pid>/ns/'
cgroup  mnt  net
$ su -c 'grep -c NSpid /proc/<pid>/status'
0
```

**判读**：客户机进程仅持有 `cgroup` / `mnt` / `net` 三个命名空间，
**没有 PID namespace**（`/proc/<pid>/status` 中 `NSpid` 字段缺失），
也没有 UTS / IPC / USER / TIME 命名空间。
⇒ **客户机不是 namespace 容器**，宿主内核只看到普通的同 uid 线程/进程。

---

## 2. 挂载：产品无专属挂载点（P1 / K1）

```text
$ su -c 'wc -l /proc/<guest_pid>/mountinfo'
152 /proc/<guest_pid>/mountinfo
$ su -c 'grep -c vphonegaga /proc/mounts'
0
$ su -c 'grep -ci instance1 /proc/mounts'
0
```

**判读**：客户机进程的 `mountinfo` 与宿主 App 完全一致（约 152 条），
**无任何产品专属挂载点**。外部未观测到“虚拟分区”以挂载形式出现。

> 与 §4.6 的对照实验对照：客户机内部的挂载树是**用户态内存态**，故宿主内核挂载表本就看不到。

---

## 3. 内存映射与文件描述符（P1 / K2）

### 3.1 readonly.bin 的真实 mmap

```text
$ su -c 'grep readonly.bin /proc/<guest_pid>/maps | head -3'
… /data/data/com.vphonegaga.titan/files/instance1/androidfs_10.0.0/system/readonly.bin
```

**判读**：客户机进程**直接 mmap 宿主明文容器文件**（真页缓存、真零拷贝）。
这与 P2 侧 `/proc/self/maps` 显示的 `/system/lib64/libnetd_client.so`（假路径/假设备号）
形成对照 ⇒ **真实 mmap + 展示层改写**。

### 3.2 memfd 现象

```text
$ su -c 'ls -l /proc/<guest_pid>/fd | grep -oE "memfd:[^ ]*" | sort | uniq -c | head'
  数百个  memfd:titan-tmp-inode-N (deleted)
```

**判读（严格）**：只能证明**存在大量匿名/临时内存对象与用户态虚拟文件实现相关**。
把“它们就是所有假 `/proc` 文件的实体”当作已证事实**超出证据边界**。
更准确的表述：该现象**与用户态 /proc 合成实现模型高度吻合**。**K2。**

### 3.3 图形设备持有

```text
$ su -c 'ls -l /proc/<guest_sf_pid>/fd | grep -oE "(/dev|/dmabuf)[^ ]*" | sort | uniq -c'
  … /dev/kgsl-3d0 /dev/ion dmabuf …
```

**判读**：客户机 SF 持有宿主 GPU 设备路径。但 `kgsl/ion/dmabuf` 是 Android 对**任何**
GPU 渲染进程的默认路径，宿主 SF 的映射数远多于客户机 SF。
内部究竟直通/代理/中转，黑盒不可区分。**K3。**

---

## 4. 存储容器格式（P1 / K1 观测 + K2 解释）

宿主私有目录 `files/instance1/androidfs_10.0.0/`：

```text
├── androidfs.bin          64 B     魔数 [REDACTED] / 裸字节 [REDACTED]
├── locales.bin           491 B
├── config.gz            1305 B
├── fscache.bin        402,698 B
├── system/
│   ├── readonly.bin   1,557,878,007 B   (1.45 GiB)  魔数 [REDACTED] / 裸字节 [REDACTED]
│   ├── superblock.bin  16 B            魔数 [REDACTED] / 裸字节 [REDACTED]
│   └── 00000000/       可写索引对象
├── vendor/  readonly.bin 30,533,337 B + superblock.bin 16 B
├── data/    831 个索引对象 + fscache.bin 67,108,864 B (64 MiB) + superblock.bin 16 B
├── cache/   superblock.bin 16 B + 00000000/（14 项）
└── root/
    ├── block.img        8,388,608 B    ← 真正的 Android boot image
    ├── readonly.bin     2,696,464 B
    └── superblock.bin          16 B
```

### 4.1 文件头解码

```text
<分区>/superblock.bin (16 B)              <分区>/readonly.bin (头 64 B)
  +0x00  42 50 55 53  "[REDACTED]"                +0x00  41 54 49 54  "[REDACTED]"
  +0x04  10 00 00 00  = 16  ← 头长度         +0x04  40 00 00 00  = 64  ← 头长度
  +0x08  9d 11 00 00  = 4509 ← 对象数        +0x08  9d 11 00 00  = 4509 ← **与 superblock 相等**
         system=4509 vendor=503
         data=3800   cache=11  root=23
```

### 4.2 关键性质

| 性质 | 证据 | 层级 |
|---|---|---|
| `readonly.bin` 头对象数 = 同分区 `superblock.bin` 对象数（逐分区精确相等） | system 4509 / vendor 503 | K1 |
| **魔数字节序** | 磁盘裸字节为 `[REDACTED]`/`[REDACTED]`/`[REDACTED]`（16 位小端半字）；`od -x` 渲染为 `[REDACTED]`/`[REDACTED]`/`[REDACTED]` | K1 |
| **`root/block.img` 是合法 Android boot image** | `[REDACTED]` + `kernel_addr=[REDACTED]` + `page_size=4096` + `name="titan"` | K1 |
| **未加密** | 映射偏移处直接读到 `7f 45 4c 46`（ELF）与合法 ARM64 指令 | K1 |
| **格式族谱** | APK 内 `libp7zip.so`（3.1 MB）含 `[REDACTED]`/`AES256CBC`/`[REDACTED]`/`BCJ2`/`Deflate64`/`PPMd` | K2 |
| **加密用在哪** | 产品自身日志 `UserKernel.log` 等为高熵密文 | K1 |

---

## 5. APK 组件元数据（P1 / K1 元数据观测）

> `/data/app/*/<pkg>*/lib/arm64/` 在无 root（P0）下为 `Permission denied`，故以下均为 P1 取得。

### 5.1 libloader64.so / libloader32.so（各约 200 KB）

```text
$ su -c '[REDACTED] -lW libloader64.so | head -20'
Elf file type is DYN (Shared object file)
Entry point [REDACTED]
Program Headers:
  INTERP  0x000270 … [Requesting program interpreter: /system/bin/linker64]
  LOAD    0x000000 … R E
  LOAD    0x02dc70 … RW
  GNU_RELRO …
```

```text
$ su -c 'strings -a libloader64.so | grep -E "too small|not supported|execv"'
"%s" is too small to be an ELF executable
%s: load executable not supported!
execv
```

**判读**：`libloader64.so` **本身就是一个 ELF 可执行文件**（`INTERP=/system/bin/linker64`），
只是顶着 `.so` 名字。如此即可放进 APK 的 `lib/arm64/`，安装时自动获得可执行位，
**不需要任何特权就能让自己的代码跑起来**。
交叉印证（P0）：`/proc/<pid>/status` 的 `Name` 字段直接就是 `libloader64.so`。

### 5.2 libuserkernel64.so（4.17 MB，用户态内核层）

```text
$ su -c '[REDACTED] --[REDACTED] -W libuserkernel64.so | awk "{print \$7}" | sort | uniq -c'
  279 条中 268 条为 UND（导入）
    0 条为定义的导出函数
```

```text
$ su -c 'strings -a libuserkernel64.so | grep -E "titan-|vma:|fscache" | sort -u'
vma:%u, dentry:%u, inode:%u, file:%u          ← 用户态 VFS 对象模型
%s/titan-memfd-inode-%lu
%s/titan-tmp-inode-%lu
fscache.bin
sys_memfd_create
titan-virtpipe-dma-%u-%d
titan-virtpipe-shm-%u-%d
titan-%u-process-monitor
titan-shell-service-socket
sl local_address remote_address st tx_queue rx_queue …   ← 伪造 /proc/net/tcp 表头
Inode RefCount Protocol Flags Type St Inode Path          ← 伪造 /proc/<pid>/fd 表头
PR_SET_SECCOMP / PR_GET_SECCOMP / PTRACE_SEIZE / PTRACE_GETREGS
```

**判读**：该模块**导入**真实 libc 调用，却**不向外界导出任何符号**；
字符串表直接给出了“用户态 VFS 对象模型”“memfd 命名”“伪造 /proc 表头”“seccomp 常量”等功能面。
这十条字符串基本就是“用户态内核”的功能清单。**K1 观测 / K2 解释。**

### 5.3 libp7zip.so（约 3.1 MB）

```text
$ su -c 'strings -a libp7zip.so | grep -E "^7z|LZMA|AES|BCJ" | sort -u'
[REDACTED]  AES256CBC  [REDACTED]  BCJ2  Deflate64  PPMd
```

**判读**：[REDACTED]/[REDACTED]/[REDACTED] 并非从零设计的加密文件系统，而是 **7-Zip 家族之上的私有封装层**。**K2。**

---

## 6. 硬件代理总线（P1 交叉印证）

```text
$ su -c 'strings -a libuserkernel64.so | grep -E "titan-virtpipe|titan-.*process"'
titan-virtpipe-dma-%u-%d
titan-virtpipe-shm-%u-%d
titan-%u-process-monitor
```

**判读**：与 P0 侧 `/proc/net/unix` 枚举到的 `@titan-pipe-1-*` / `@titan-process-worker-server-1-*`
命名**完全对应**，交叉印证外设代理通道的真实性。

---

## 7. 归档说明

| 项目 | 说明 |
|---|---|
| 本归档状态 | **保留**（不因当前载体无 root 而删除） |
| 可复现性 | 需 P1（宿主 root）；当前主载体不可复现，故归档 |
| 与对照实验的关系 | P1 观测与 P0/P2 对照实验**互补**：P1 给出“静态形态”（文件格式、符号表、命名空间），对照实验给出“动态行为”（返回值差异） |
| 更正记录 | 本版曾一度删除本归档，已恢复（见 README §10.4） |

---

*归档结束。以上均为早期带 root 对照载体上的真实回显，原样保留。*

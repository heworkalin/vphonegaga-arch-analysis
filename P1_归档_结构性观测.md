# P1（宿主 root）层结构性观测 · 归档

| 项目 | 内容 |
|---|---|
| 归档日期 | 2026-09-23 |
| 层级 | **P1** —— 宿主 `uid=0(root)`，经 `su -c`（Magisk Alpha） |
| 载体 | Redmi K30 5G（`picasso`），LineageOS 22.2 UNOFFICIAL，Magisk Alpha |
| 定位 | **仅 P1 可达的、结构性观测**归档。**不含字节级取证**（魔数 / 偏移 / 符号表 / 字符串摘录均属取证，不属架构分析） |
| 关联 | [`README.md`](README.md) §2.2 / §4.2 / §4.4 / §4.9 / §4.13 |

> P1 与 P0/P2 的差别在于**可观测范围**：`/proc/<pid>/{ns,maps,fd,environ}`、
> `/data/data/<pkg>/` 等在无 root 时不可读。本归档只记录这些范围内**对架构有意义的结论**。

---

## 1. 命名空间：无内核级隔离

```text
$ su -c 'ls /proc/<pid>/ns/'
cgroup  mnt  net
$ su -c 'grep -c NSpid /proc/<pid>/status'
0
```

**结论（K1）**：客户机进程仅持有 `cgroup` / `mnt` / `net`，**没有 PID namespace**
（`NSpid` 字段缺失），也没有 UTS / IPC / USER / TIME。
⇒ 客户机**不是 namespace 容器**，宿主内核只看到一批同 uid 的普通进程。

---

## 2. 挂载：产品无专属挂载点

```text
$ su -c 'wc -l /proc/<guest_pid>/mountinfo'
152 /proc/<guest_pid>/mountinfo
$ su -c 'grep -ciE "vphonegaga|instance1" /proc/mounts'
0
```

**结论（K1）**：客户机进程的 `mountinfo` 与宿主 App 一致，
**宿主内核挂载表中没有任何产品专属挂载点**。
⇒ 客户机的挂载视图不在宿主 VFS 中（与 §4.6 的客户机侧 uid 扫描相互印证）。

---

## 3. 内存映射与描述符

### 3.1 容器文件的真实映射

```text
$ su -c 'grep -i readonly /proc/<guest_pid>/maps | head -3'
… /data/data/com.vphonegaga.titan/files/instance1/…/system/readonly.bin
```

**结论（K1）**：客户机进程**直接映射宿主侧的私有容器文件**（真页缓存、可共享）。
这与 P2 侧 `/proc/self/maps` 显示的 `/system/lib64/...`（假路径、假设备号）形成对照
⇒ **真实映射 + 展示层改写**。

### 3.2 匿名/临时内存对象

```text
$ su -c 'ls -l /proc/<guest_pid>/fd | grep -c memfd'
数百
```

**结论（K2）**：宿主 fd 表中存在大量匿名/临时内存对象，
**与用户态虚拟文件实现模型高度吻合**。
把它们当作"所有假 `/proc` 文件的实体"属**超出证据边界**。

### 3.3 图形设备持有

**结论（K3）**：客户机 SurfaceFlinger 持有宿主 GPU 设备路径。
但 `kgsl / ion / dmabuf` 是 Android 对**任何** GPU 渲染进程的默认路径，
内部究竟直通 / 代理 / 中转，黑盒不可区分。

---

## 4. 存储层（结构级）

**结论（K1 观测 + K2 解释）**：

- 客户机镜像**不通过内核挂载**，由宿主侧运行时以**私有容器格式**承载；
- 该格式**不加密**（客户机进程可直接只读映射，映射内容为明文）；
- **分卷组织**（只读数据卷 + 索引），另有可写层与缓存层；
- 客户机内部的 **boot 分区也由容器提供**（可解析为 Android boot 结构）。

> 字节级细节（文件头字段、魔数、偏移、体积）**不在本报告范围内**。

---

## 5. 归档说明

| 项目 | 说明 |
|---|---|
| 保留理由 | P1 观测**当前载体不可复现**（主载体无 root），故归档 |
| 保留范围 | **仅结构性结论**；字节级取证已移除 |
| 与对照实验的关系 | P1 给"静态结构"，对照实验给"动态行为"，二者互补 |
| 更正记录 | 本版曾一度删除本归档，已恢复（见 README §10.4） |

---

*归档结束。以上均为早期带 root 对照载体上的观测，按结构性结论整理。*

# 垫片分层模型：进程与线程（实测）

| 项目 | 内容 |
|---|---|
| 记录日期 | 2026-09-23 |
| 方法 | 双设备对照：宿主侧读真实线程，客户机侧读 vpid 线程，按 vpid 配对 |
| 样本 | 87 对匹配（客户机可见进程） |
| 关键判据 | **`host 线程数 = guest 线程数 + 1`** |
| 结论 | **guest 进程 ↔ host 进程 1:1；guest 线程 ↔ host 线程 1:1；每进程外加 1 个 shim 监督线程** |

> 本文只描述**可观测的进程/线程结构**。标【K1】的是实测，标【K2】的是推断。

---

## 0. 一句话

> **它不是"一个进程里跑一大堆模拟线程"，而是：**
> **每个 guest 进程就是一个真实 host 进程；guest 的线程就是 host 的线程；
> 每个进程只额外挂 1 个 shim 监督线程；协调交给一个独立的 broker 进程（带 fd 工作池）。**

---

## 1. 实测证据

### 1.1 进程层【K1】

- 每个 `titan{64,32}_<vpid>:<name>` 是**独立 host 进程**：
  - `Tgid == Pid`
  - **各自独立的 `VmSize`**（⇒ 各自独立 `mm_struct`，**不共享地址空间**）
- 拓扑：

```
3968  com.vphonegaga.titan          (App 主进程, ~83 线程)
 └ 4645  com.vphonegaga.titan:instance1  (实例容器, ~93 线程)
    └ 4928  titan64_0:kernel        (虚拟内核 / broker, 16 线程)
       ├ 4967  titan32_0:kernel     (32 位内核链路, 1 线程)
       ├ 4986  titan64_1:init
       ├ 4998  titan64_3:init
       ├ 5122  titan64_15:logd
       └ … 约 85–100 个 guest 进程
```

### 1.2 ★ 线程层：`host = guest + 1`【K1，核心判据】

按 vpid 配对宿主机与客户机的 `Threads` 字段：

| 统计 | 结果 |
|---|---|
| 匹配对数 | **87** |
| `host = guest + 1` | **86 对** |
| 其它 | 1 对（vpid 117，diff=0，疑似瞬时线程已退出）|

部分样本：

| vpid | 进程 | host 线程 | guest 线程 | 差 |
|---|---|---:|---:|---:|
| 1 | init | 2 | 1 | **+1** |
| 15 | logd | 7 | 6 | **+1** |
| 24 | vold | 6 | 5 | **+1** |
| 34 | apexd | 4 | 3 | **+1** |
| 43 | magiskd | 6 | 5 | **+1** |
| 56 | lspd | 22 | 21 | **+1** |
| 59 | netd | 15 | 14 | **+1** |
| 61 | zygote64 | 7 | 6 | **+1** |
| 102 | surfaceflinger | 13 | 12 | **+1** |
| 109 | system_server | 15 | 14 | **+1** |

### 1.3 线程名映射（以 `logd` 为例）【K1】

| host PID/TID | host 线程名 | 对应 guest 线程名 |
|---|---|---|
| 5122 | `libloader64.so` | `logd`（主线程）|
| **5123** | **`libloader64.so`** | **（guest 中不存在）← 这就是多出来的那 1 个** |
| 5155 | `logd.daemon` | `logd.daemon` |
| 5156 | `logd.reader` | `logd.reader` |
| 5158 | `logd.writer` | `logd.writer` |
| 5159 | `logd.control` | `logd.control` |
| 6125 | `logd.reader.per` | `logd.reader.per` |

⇒ **guest 的线程在宿主内核里就是真实线程**；每个进程**恰好多 1 个**（名称为 `libloader64.so`）。

### 1.4 broker 的工作池【K1】

`titan64_0:kernel`（host PID 4928）= **16 线程**：

```
4928  libloader64.so     ← 主线程
4932  fd_worker_0        ← ★
4934  fd_worker_1        ← ★
4938  fd_worker_2        ← ★
4939  fd_worker_3        ← ★
4943  fd_worker_4        ← ★
4944  fd_worker_5        ← ★
4945  fd_worker_6        ← ★
4954  fd_worker_7        ← ★  8 个 fd 工作线程
4957  libloader64.so
4959  libloader64.so
4960  libloader64.so
4961  libloader64.so
4962  libloader64.so
4966  libloader64.so
4972  libloader64.so
```

- **8 个 `fd_worker_N`** 是一个**文件描述符工作线程池**（直接命名证据）。
- `titan32_0:kernel`（4967）= **1 线程**（32 位链路极简）。

---

## 2. 分层模型【K2 推断】

```text
宿主 Android（真实内核：调度 / mm / 页缓存 / 驱动）
   │
   ├─ Layer 1  com.vphonegaga.titan        App 主进程（UI / 实例管理）
   │
   ├─ Layer 2  :instance1                  实例容器（宿主侧运行时）
   │
   ├─ Layer 3  titan64_0:kernel            ★ 中央 broker
   │             ├─ fd_worker × 8          ← fd 工作池
   │             ├─ 主线程 / 协调线程
   │             └─ titan32_0:kernel       （32 位链路，独立 1 线程）
   │
   └─ Layer 4  N 个 guest 进程（各自独立 mm、独立 tgid）
         每个 guest 进程内部：
         ┌────────────────────────────────────────────┐
         │ host 进程（真实 mm / 真实调度 / seccomp 2 层）│
         │  ├─ 主线程            ← 运行 guest 主线程     │
         │  ├─ ★ shim 线程 ×1    ← 多出来的那 1 个       │
         │  └─ guest 线程 ×N     ← 1:1 对应 guest 线程   │
         └────────────────────────────────────────────┘
```

**三条映射规则**：

| 层级 | 映射 |
|---|---|
| 进程 | guest 进程 ↔ host 进程，**1:1** |
| 线程 | guest 线程 ↔ host 线程，**1:1（进程内）** |
| 附加 | 每进程 **+1 shim 线程**；全局 **+1 broker 进程**（含 fd 池）|

---

## 3. 为什么这样设计有优势【建议】

| 设计 | 好处 |
|---|---|
| guest 进程 = host 进程 | 地址空间、调度、页缓存全部由宿主内核原生承担，**不需要模拟** |
| guest 线程 = host 线程 | 同步/等待/竞态交给宿主内核，**不需要用户态线程调度器** |
| 每进程只加 1 个监督线程 | 拦截开销**每进程常数级**，不随 guest 线程数膨胀 |
| broker 独立进程 + fd 池 | fd 操作集中批处理，避免每个进程各搞一套 |
| 32 位链路独立 | 32/64 位 ABI 分离，互不干扰 |

---

## 4. 与"共享 `mm_struct`"模型的对比

| | 共享 mm 模型（外部 AI 提出） | 本实测模型 |
|---|---|---|
| 进程关系 | 多 tgid 共用 1 个 mm | **各自独立 mm** |
| 判据 | — | `VmSize` 全不同（471,960 ~ 10,962,564 kB）|
| 线程关系 | 未说明 | **1:1 + 1 shim** |
| 结论 | ❌ 证伪（见 [`假设验证_共享mm证伪.md`](假设验证_共享mm证伪.md)） | ✅ K1 |

> 这也说明：**"看起来更复杂"的模型不一定是对的**；一个 `Threads` 字段的批量对照就能判定。

---

## 5. 未确认项（诚实边界）

| # | 未确认 | 层级 |
|---|---|---|
| 1 | 那 1 个 shim 线程的**具体职责**（IPC 服务端？seccomp `USER_NOTIF` 监听？） | K3 |
| 2 | broker 与各 guest 进程之间的 **IPC 协议 / 通道** | K3 |
| 3 | `fd_worker_N` 的**具体工作内容**（fd 转发？路径解析？） | K3 |
| 4 | 线程名在 host 侧显示为 `libloader64.so` 的线程，是"未命名"还是"被刻意统一" | K3 |
| 5 | 1 例 `diff=0`（vpid 117）是否为稳态现象 | K3 |

---

## 6. 复现命令

```bash
H=127.0.0.1:5555   # 宿主
V=127.0.0.1:6556   # 客户机

# 判据一：host = guest + 1
# host 侧
adb -s $H shell 'for p in $(ps -A -o PID,NAME | grep -E "titan(64|32)_[0-9]+:" | awk "{print \$1}"); do
  nm=$(cat /proc/$p/cmdline 2>/dev/null | tr "\0" " ")
  v=$(echo "$nm" | sed "s/^titan[0-9]*_//" | sed "s/:.*//")
  t=$(cat /proc/$p/status 2>/dev/null | awk "/^Threads:/{print \$2}")
  case "$v" in ""|*[!0-9]*) continue;; esac
  echo "H $v $t"
done'
# guest 侧
adb -s $V shell 'for d in /proc/[0-9]*; do
  v=$(basename $d); t=$(cat $d/status 2>/dev/null | awk "/^Threads:/{print \$2}")
  [ -n "$t" ] && echo "G $v $t"
done'
# 配对后 diff 应为 +1

# 判据二：broker 的 fd 工作池
adb -s $H shell 'p=$(ps -A -o PID,NAME | grep "titan64_0:kernel" | awk "{print \$1}")
  for t in /proc/$p/task/*; do cat $t/comm; done | sort | uniq -c | sort -rn'

# 判据三：独立 mm（不共享）
adb -s $H shell 'for p in <PID1> <PID2>; do
  cat /proc/$p/status | awk "/^Name:|^VmSize:/{printf \"%s \", \$2} END{print \"\"}"; done'
```

---

*记录结束。【K1】为实测，【K2】为推断，【建议】为设计意见。*

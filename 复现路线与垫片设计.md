# VPhoneGaGa 复现路线与垫片设计（含高性能设计）

| 项目 | 内容 |
|---|---|
| 文档定位 | **工程构想文档**：基于已取证事实，讨论"如何造一个类似的东西"，并**重点回答如何做到高性能** |
| 前提 | 本系列**不再做进一步逆向**；只使用已验证的架构与选型结论 |
| 关联 | [`README.md`](README.md) §5.5（跨进程垫片运行时）· [`ARCHITECTURE.md`](ARCHITECTURE.md) |
| 确定性标注 | **K1** 已实测事实 · **K2** 候选模型 · **K3** 推测/未验证 · **建议** 本文档给出的设计意见（非产品事实） |

> ⚠️ **重要边界（请先读）**：
>
> - 标【**K1**】的 = **实测事实**（可由命令复现）；
> - 标【**K2 / K3**】的 = **推断 / 未验证**；
> - 标【**建议**】的 = **本文档自己的设计意见**，**不是**产品事实。
>
> **本文件不是“VPhoneGaGa 内部实现还原”。**
> 它是一份**独立复现设计**：根据已验证的**外部行为**，提取其**可复用的架构思想**，
> 再由我们自己给出工程方案。**方案与产品内部实现不要求相同，也无从断言相同。**
>
> 上下游关系：
> `README.md`（我们观察到了什么 · K1/K2/K3）→ **本文档**（基于这些事实，我们会怎么设计）。

---

## 0. 一句话结论

> ⚠️ **更正**：早期版本把它的本质写成“少做事”，**这是错的**。
> 准确的说法是：
>
> **它不是“少做事”，而是“把大一统拆成许多只做最小工作的模块，再花最大力气把它们重新缝成一个统一系统”。**
>
> - **看起来**：一个统一的东西（guest 看到一台完整 Android）——但这只是**最外层的表象**；
> - **内部**：每个模块只干最少的事（最小职责、彼此解耦）；
> - **代价**：模块之间必须被一个统一的垫片 / broker **重新粘合**。
>   ⇒ **“统一”是整件事里最便宜的表象，也是最昂贵的实质。**
>
> **它省的只是“快路径上的每调用工作”**（默认 `ALLOW`，用户态不参与）；
> **它更多的却是“架构上的总体工作”**：省下的每一分“每模块的工作”，
> 都要在协调层（broker / IPC / 上下文一致性）上**加倍还回去**。

### 0.1 大一统 vs 最小职责拆解（关键区别）

| 维度 | 大一统 VM（QEMU / gVisor） | 本产品 |
|---|---|---|
| 形态 | **一个组件装下全部** | **许多只做最小工作的模块** |
| 复杂度位置 | **集中**（VMM / sentry 内部） | **分散 + 一个协调层** |
| 模块间交互 | 同地址空间**直接调用** | **跨进程 IPC** |
| 状态 | 集中一处 | 分散，且**必须随时保持一致** |
| 看起来 | 复杂 | 简单（“统一的垫片”）|
| 实际上 | 复杂度**可控** | **统一的表象最便宜，维持统一最昂贵** |

> **所以“统一的垫片”只是最外层的表象，也是最简单的部分；“真正复杂的，是从它往里才开始”。**
>
> **复现的关键不是“写一个内核”，也不是“划定一条窄拦截带”那么简单，而是：
> 如何在“每个模块只做最少事”的前提下，把它们缝成一个对外一致的统一系统。**

### 0.2 本文档真正想复现的东西

> 不是“写一个 Linux 内核”，也不是“写一个 syscall hook”，而是：

```text
                Guest Android
                      │
          多个真实 Host carriers（进程/线程）
                      │
                      ▼
            ┌──────────────────┐
            │ Per-process shim │   ← 捕获 + 本地快速答复
            └────────┬─────────┘
                     │
              fast path（默认直通）
              /                    \
         Host 侧                 Broker 侧
      （真实资源）             （语义状态）
         mmap / VMA               vPID / uid / cap
         CFS 调度                 /proc 视图
         GPU / socket             mount tree
              \                    /
               └────────┬─────────┘
                        ▼
              一个统一的 Guest 世界
```

> **统一是目标，拆分是手段，协调是代价。**

---

### 1.1 三个平面【K1 观察结果】

| 平面 | 处理方式 | 实测证据 |
|---|---|---|
| **合成面** | 用户态直接答复，不进内核 | `uname()` 返回 `4.14.42-titan`；`sysinfo()` 返回 3.9 GB；身份类返回虚拟 uid |
| **虚拟对象面** | 用户态维护状态 | 挂载树（uid 10000 也能 mount）；`/proc/net/tcp` 被投影为空；`/proc/mounts` 重新拼装 |
| **宿主承载面** | 直接借宿主能力 | `clock_gettime` 两侧秒值**完全相同**；socket 出现在宿主 `/proc/net/tcp`；`readonly.bin` 真实 mmap |

### 1.2 进程模型：观测事实 vs 候选模型

**观测事实【K1】**：

```text
常规 VM:   1 主进程 + N vCPU 线程   （QEMU / gVisor）  ← 已知
本产品:    客户机对应约 85–100 个宿主可见进程
          （titan{32,64}_<vpid>:<name>；每个 Guest 任务对应一个真实 Host 进程）
```

**候选模型【K2】**（架构解释，非直接观测）：

```text
N 个 Host 进程承载 Guest execution
        ↓
  per-process shim（进程内垫片）
        ↓
  central broker / 子系统 worker
```

> ⚠️ **边界说明**：**“每进程内垫片”与“中央 broker”目前没有被直接观测到**。
> 它们是与观测高度吻合的 **候选解释**【K2】。
> **不应被读作“已证实的内部结构”。**

**为什么我们会选择多进程**【建议】：

要复用宿主的调度 / 内存 / GPU / socket，就不应把 guest 进程塞进单一地址空间
（塞进去就要自己做调度与内存管理，等于重写内核）。
因此设计上选择：**用宿主进程当载体，把语义处理抽到本地垫片 + 协调层。**

### 1.3 拦截边界【K1 + K2】

- 第二层 **seccomp** 过滤器（`Seccomp_filters` 1→2），由虚拟内核安装，全树继承。
- `TracerPid` 恒为 0 ⇒ **不是 ptrace 路线**。
- ⚠️ 具体策略（`ALLOW` / `USER_NOTIF` / `TRACE` / `ERRNO`）未判定【K3】。

**设计上最关键的一点**【建议】：

> seccomp BPF **无法检查字符串参数**（如路径）。因此"按路径拦截 `openat`"不可能纯靠 BPF 规则完成。
> 合理设计是：**默认 `SECCOMP_RET_ALLOW`，仅对一小撮需要语义处理的系统调用返回 `USER_NOTIF`，
> 由用户态 supervisor 读取参数（包括路径）再决定**。

### 1.4 事实与设计必须分开（本文件的核心纪律）

> 本文档最容易犯的错误，就是把“我们希望采用的选型”当成“产品已验证的事实”。
> 两者必须彻底拆开：

| | **VPhoneGaGa 的事实**（外部可观测） | **我们的技术选型**（设计意图） |
|---|---|---|
| 进程 | 多 Host 进程承载 Guest | 多进程 execution carrier |
| 拦截 | Guest 语义投影（`/proc`/身份/挂载） | per-process shim + broker + IPC |
| 数据面 | Host 资源复用（mmap/socket/GPU） | Host fast path（默认直通） |
| 文件 | 虚拟 VFS / 挂载树 | virtual semantic layer |
| 网络 | 网络中介（控制面被拦 + 信息面被投影） | 数据面借用 + 控制面白名单 |
| 系统 | Android 用户态 | AOSP 镜像 + 自建 loader |

**两者不要求相同。** 我们应该借用的是它的**架构思想**，而不是去精确复刻它的内部。

---

## 2. 复现路线（分阶段）

| 阶段 | 目标 | 验收标准 | 预计投入 |
|---|---|---|---|
| **P0** | app 沙箱内 exec 静态 ELF + seccomp 拦 `openat` + 伪造 `/proc/self/status` | 一个静态二进制在无 root 的 app 内跑起来，且读到假 uid | 1–2 人月 |
| **P1** | 最小用户态内核：seccomp + 用户态 VFS + 路径重定向 | 跑起 busybox，`ls /system` 走重定向 | 6–12 人月 |
| **P2** | ⭐ **broker + IPC（本方案的工程分水岭）** | 多进程共享 vPID/uid/fd/mount 上下文；fork/exit 正确镜像 | 6–12 人月 |
| **P3** | 自研 ELF loader（32 + 64 位） | 加载并运行 Android `init` | 6–12 人月 |
| **P4** | 存储容器（页对齐、可直接 mmap）+ 可写层 | 容器可页对齐映射；【建议】验证代码页可跨进程复用 | 3–6 人月 |
| **P5** | AOSP 编译 Android，跑起 init + core services | `init` → `vold`/`logd`/`servicemanager` 起来（不含 zygote） | 12–24 人月 |
| **P6** | 图形 + framework | `zygote` + `system_server` 起来，单窗口显示 | 12–24 人月 |
| **P7** | Magisk 兼容层 + 自建 `su` | 解析 boot payload 拉起 magiskd；独立 root 通道 | 6–12 人月 |
| **P8** | 外设代理 + 机型适配 | 相机/GPS/传感器/指纹等 13 个子系统 | 12–36+ 人月 |

> **【建议】P2 是本复现方案的工程分水岭**：P2 之前是“写拦截器”，P2 之后是“写分布式运行时”。
> 预计大多数尝试会死在 P2 的上下文一致性上。（这是**对复现工程的判断**，不是产品事实）

---

## 3. 垫片设计（各子系统怎么做）

### 3.1 进程内垫片（`libloader` 角色）

| 职责 | 做法【建议】 |
|---|---|
| 被装载 | 打成 `.so` 放进 APK `lib/<abi>/` 以取得可执行位（借安装时自动加权限） |
| syscall 捕获 | 由 seccomp `USER_NOTIF` 把需要处理的调用抬到 supervisor；**其余直接 `ALLOW`** |
| 参数采集 | 读寄存器 / 用户栈（注意 32/64 位 ABI 差异） |
| 结果回填 | 改写返回值寄存器 / 目标内存 |
| 快速返回 | 能在本地决定的（身份、部分 `/proc`）**不要 IPC**，直接就地答复 |

### 3.2 中央 broker

| 职责 | 要点 |
|---|---|
| 上下文表 | `vpid → {uid, cap, cwd, fd table, mount view, mm}` |
| 分发 | 按业务类型路由到子服务（VFS / mount / net / proc） |
| 生命周期 | fork / exec / exit / 孤儿回收 / 消息清理 |
| 一致性 | **所有载体进程看到的视图必须一致**（最难） |

### 3.3 用户态 VFS 与挂载树

| 设计点 | 说明 |
|---|---|
| 对象模型 | `dentry / inode / file`，必须支持 **mmap**（不能只做 read/write） |
| 路径重定向 | `/system/...` → 容器内偏移；**解析结果要缓存** |
| 一致 inode | 同一文件的 (dev, ino) 在所有 guest 进程间保持一致 |
| 挂载树 | 全用户态维护；能力用**白名单**（产品实测：`/proc/filesystems` 仅 7 项，列表外 `EINVAL`）|
| 权限 | 可自定义（产品实测：`mount` 对所有 uid 放行，`umount` 仅 root —— **不对称**）|

### 3.4 `/proc` 合成

- **按需 + 缓存**：实测 `/proc/mounts` 两次 md5 相同 ⇒ 有缓存。
- **memfd 预生成**：宿主 fd 表有大量 `memfd:titan-tmp-inode-N` ⇒ 把合成结果写进 memfd，读取时直接 mmap。
- 必须逐字段做对（最耗时的苦工），且**多接口要自洽**（产品自己在这里露馅：`mount` 与 `/proc/mounts` 设备名不一致）。

### 3.5 网络：数据面 / 控制面分离【K1】

| 面 | 设计【建议】 |
|---|---|
| 数据面 | **直接用宿主 socket**（实测：guest 端口出现在宿主 `/proc/net/tcp`）→ **避免额外的用户态网络数据面转发**；实际吞吐仍受宿主 socket / 调度 / 网络栈 / 代理层影响 |
| 控制面 | `setsockopt` 等做**白名单**（实测：`IP_RECVTTL`/`IP_PKTINFO`/`IP_RETOPTS` 被拦）|
| 信息面 | `/proc/net/*` 投影/过滤（实测：guest 内部为空）|

### 3.6 Magisk 兼容层与自建 `su`【K1 观测 + K2 推测】

- **自建 `su`**：四状态恒在，由 init 拉起，独立开关 ⇒ **root 通道自建，Magisk 只是叠加**。
- **Magisk 兼容层（K2）**：推测为"解析 boot 镜像中的 Magisk 负载 → 自实现兼容方式拉起 `magiskd`"。
  证据：magiskinit 的父进程是客户机 init；Magisk 阶段固定在 `apexd ↔ netd` 之间。
- ⚠️ **兼容范围未知【K3】**：只实测 26.0；更高版本行为未验证。

---

## 4. 高性能设计（本文重点）

### 4.1 可能的结构性优势【K1 观测 + 建议】

| 来源 | 机制 | 实测依据 | 性质 |
|---|---|---|---|
| **数据面宿主直通** | `mmap` 是真实宿主映射 | 客户机映射 `readonly.bin`，(dev,ino) 真实 | K1 |
| **调度用宿主 CFS** | guest 进程是真实宿主进程 | ~100 个宿主进程，继承 `top-app` | K1 |
| **代码页复用** | 明文容器 + `MAP_SHARED` / 页缓存 | `readonly.bin` 明文可 mmap | **【建议】**（“跨进程共享”是设计推论，非实测）|
| **GPU 走宿主驱动** | 客户机 SF 持有宿主 GPU 设备 | `/dev/kgsl-3d0` / `/dev/ion` | K1 持有；**处理位置 K3** |
| **合成面不进内核** | `getuid`/`uname` 用户态直接返回 | 对照实验：客户机返回合成值 | K1（**不等于“比内核快”，未做 benchmark**）|

> **核心洞察（更正）**：它**避免了一部分数据面上的额外虚拟化路径，但这不等于“没有虚拟化开销”**。
>
> 语义投影、IPC、状态同步、`/proc` 合成、mount、权限、socket 控制面——**这些都是成本**。
> 更准确的表述是：
>
> **它把开销从高频的数据面，转移到了低频的语义面与协调面。**
>
> ⚠️ 本报告**未做任何 benchmark**，因此不能得出任何性能结论；
> 上述只是**机制层面的结构性判断**。

### 4.2 性能代价在哪：必须优化的四件事

#### ① seccomp 拦截的每调用开销 ⭐ 最关键

**建议：默认 `ALLOW`，选择性 `USER_NOTIF`。**

```text
seccomp 过滤器
  ├─ 绝大多数 syscall（mmap / futex / clock_gettime / read / write / ...）→ SECCOMP_RET_ALLOW
  │     ⇒ 不产生 USER_NOTIF 所需的用户态往返
  └─ 需要语义处理的少数（openat / uname / sysinfo / socket 控制面 / mount）→ SECCOMP_RET_USER_NOTIF
        ⇒ 只有这些才付“陷入用户态”的代价
```

- `ALLOW` 路径：**不会产生 `USER_NOTIF` 所需的用户态往返**；
  其主要开销仍是**正常的 syscall / seccomp 检查本身**。
  **这是性能的主要来源，但不等于“零成本”。**
- 反面教材：若对**全部** syscall 都 `USER_NOTIF`，每个调用都要陷入用户态 → 性能崩塌。
- 策略选择对比：

| 策略 | 相对开销 | 能做什么 |
|---|---|---|
| `ALLOW` | 低（仅正常 syscall/seccomp 检查） | 直通 |
| `ERRNO` | 低 | 只能拒绝，**不能改写参数/返回值** |
| `USER_NOTIF` | 中（用户态往返） | 可读参数（含路径）、可改写返回值 ← **主力** |
| `TRACE` | 高（每次 ptrace stop） | 能力最全，但最慢；产品**没用**（`TracerPid=0`）|

#### ② IPC 开销

**建议：状态本地优先，跨进程次之。**

- **本地决定优先**：身份、部分 `/proc` 能在进程内垫片算出答案的，**绝不 IPC**。
- **共享内存 + 无锁环形队列**：不要把每次交互都做成 `send/recv` 系统调用。
- **批量/异步**：合并同类请求；允许乱序完成（按调用序号回填）。
- **只在必要时同步**：只有调用方必须等待结果的才阻塞。

#### ③ `/proc` 合成开销

**建议：预生成 memfd + 内容缓存 + 变更失效。**

- 静态内容（`cpuinfo`/`version`）→ 一次生成，长期缓存。
- 半静态（`mounts`/`status`）→ 状态变更时标记失效，下次读取重建。
- 用 **memfd** 承载，读取路径变成"拿到已存在的 fd"，避免每次拼接字符串。
  （产品实测：宿主 fd 表有大量 `memfd:titan-tmp-inode-N` ⇒ 与模型吻合）

#### ④ 路径重定向开销

**建议：解析结果缓存 + 前缀树匹配。**

- `/system/...` → 容器的映射结果要缓存，避免每次字符串比较。
- 用**前缀树 / 哈希**而不是线性规则表。
- 保持 (dev, ino) 一致性只需在首次解析时确定并缓存。

### 4.3 性能设计三条铁律

| # | 铁律 | 含义 |
|---|---|---|
| 1 | **快路径不引入额外用户态往返** | 不需要改语义的调用，应走 `ALLOW`；但仍有正常 syscall/seccomp 成本 |
| 2 | **默认直通，白名单拦截** | 拦截是例外而非常态；新增拦截必须证明"非拦不可" |
| 3 | **状态本地优先** | 能在进程内答的问题不要跨进程；跨进程的必须可批量 |

### 4.4 性能自检清单（实现时逐条验证）

- [ ] 有没有哪个高频 syscall 被误拦？（用 `strace`/计数器统计拦截率）
- [ ] 拦截率是否控制在个位数百分比？超过 20% 就要重新审视设计
- [ ] 每次 `USER_NOTIF` 的处理路径是否都 < 微秒级？
- [ ] 是否存在"每次读取都重新合成"的 `/proc` 文件？
- [ ] fork/exec 的上下文镜像是否避免了 O(全部状态)？
- [ ] IPC 是否使用了共享内存而非逐次 socket？
- [ ] `mmap`/页缓存是否真正跨进程共享（而非复制）？

---

## 5. 开源选型表（能借的零件）

| 子系统 | 必须自写 | 可借开源 | 备注 |
|---|:---:|---|---|
| seccomp 拦截层 | ✅ | `libseccomp`（仅封装）、gVisor `platform/seccomp` | 策略决定性能上限 |
| 进程内 loader | ✅ | musl `ldso`、bionic `linker`、`libelf` | RELA/RELR/TLS/GNU_RELRO、双架构 |
| **broker + IPC** | ✅ | **无现成件** | **最难，也最没捷径** |
| 用户态 VFS | ✅ | `libfuse`（架构）、EROFS（格式思路） | 必须支持 mmap |
| `/proc` 合成 | ✅ | 无 | 苦工 |
| mount tree | ✅ | 无 | 白名单 + 权限表 |
| socket 控制面 | ✅ | 无 | 白名单 + 投影 |
| 存储容器 | ✖ | **7-Zip / LZMA SDK**、EROFS 格式 | 页对齐可 mmap |
| Android 用户态 | 部分 | **AOSP** | HAL / SELinux / Property / Binder |
| Magisk 兼容层 | ✅ | Magisk 源码（参考行为） | 版本敏感 |
| 图形 | ✅ | SurfaceFlinger、gralloc/mapper HAL | 绑宿主 vendor HAL |
| 外设代理 | ✅ | 各 HAL | 13 个子系统 |

**开源对照**：

| 项目 | 差距 |
|---|---|
| redroid / Waydroid / Anbox | 需 root + 内核模块 |
| QEMU / Cuttlefish | 硬件虚拟化，性能不可比 |
| gVisor | 有 seccomp/sentry 思路，无 Android / 图形 |
| proot | 不拦 syscall，伪造不了 `/proc` |
| **两仪 Twoyi** | **最接近**，但拦截深度 / 镜像格式 / 双架构更浅 |

---

## 6. 风险与卡点（会死在哪）

| # | 卡点 | 说明 |
|---|---|---|
| 1 | **跨进程上下文一致性** | fork/exec/exit 镜像 + 多进程视图一致；**开源无现成答案** |
| 2 | **`/proc` 全字段自洽** | 逐文件苦工，没有捷径（产品自己都露馅） |
| 3 | **Magisk 兼容** | 解析 boot payload + 版本敏感 |
| 4 | **无 root 自举** | app 沙箱 + SELinux + seccomp + targetSdk 限制下让代码跑起来 |
| 5 | **图形/外设绑宿主 vendor HAL** | 跨机型成本线性增长 |
| 6 | **性能退化** | 拦截率失控 → 逐步变成"每个 syscall 都陷入用户态" |

---

## 7. 结论

1. **它的本质不是“少做事”，而是“拆成最小职责 + 再缝回统一系统”。**
   看起来是统一的（guest 看到一台完整 Android），内部却是许多只做最少工作的模块。
2. **“统一的垫片”是最简单的表象，真正复杂的是从它往里开始的协调层。**
3. **【建议】P2（broker + IPC）是本复现方案的工程分水岭**：它是主要工程量，也是主要风险——
   因为“把拆开的模块重新缝成统一”这件事就发生在这里。
4. **性能与工程量是两个相反的账**：
   - 性能上：**默认直通、选择性拦截**（用 `ALLOW` 避开不必要的用户态往返，用 `USER_NOTIF` 只处理必须改写的少数调用）；
   - 更准确的说法：**把开销从高频数据面转移到低频语义面与协调面**（不等于“没有开销”）；
   - 工程量上：**比大一统 VM 大得多**，省下的“每模块工作”全部转移到协调层。
5. **数据面尽量借宿主**（mmap / 页缓存 / CFS / GPU / socket）——既减少额外路径，也省工程量。
6. **真正无法借的**：broker 的跨进程一致性、`/proc` 全字段、Magisk 兼容层、无 root 自举。

> **一句话**：做一个“运行在普通 Android app UID 下的 Android 用户态执行环境”，
> 把功能**尽可能拆成最小职责的模块**，再用一层垫片 + 协调层把它们**重新缝成一个对外统一的系统**。
> 难点不在拦截，也不在“少做事”，而在：
> **在保持模块最小化的同时，维持一套跨进程、随时一致的统一表象。**

> ⚠️ **本文件的定位**：以上均为**独立复现设计**——基于已验证的外部行为提取可复用的架构思想，
> 不代表也不断言 VPhoneGaGa 的内部实现。

---

*文档结束。标【K1】的为实测事实，标【K2/K3】的为推断，标【建议】的为本文档设计意见。*

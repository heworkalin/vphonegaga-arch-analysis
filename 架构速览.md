# VPhoneGaGa 架构速览（一分钟读懂）

| 项目 | 内容 |
|---|---|
| 用途 | **快速理解**：本仓库讲了什么、它的思路是什么、优劣在哪 |
| 性质 | ⚠️ **推测性总结（K2/K3）** —— 基于公开信息与黑盒观测的**候选模型**，**不代表产品真实实现** |
| 详细版 | [`README.md`](README.md) §5 架构模型 · [`垫片分层模型_进程与线程.md`](垫片分层模型_进程与线程.md) |
| 声明 | 本仓库为**未获授权的第三方分析**，一切以官方为准；文本许可见 [`LICENSE`](LICENSE) |

---

## 一句话

> **它没有"造一个内核"，也没有"写一个 syscall hook"，而是：**
> **把一台 Android 拆成很多个"只做最少工作"的模块，再用一层垫片把它们缝成一个统一的系统。**

---

## 三条核心思路（推测）

| # | 思路 | 一句话 |
|---|---|---|
| **1** | **多进程承载** | 每个客户机进程就是一个**真实宿主进程**；客户机的线程就是宿主线程 |
| **2** | **边界拦截** | 用**第二层 seccomp** 做拦截边界：**默认直通，选择性拦截** |
| **3** | **语义分流** | 每次请求按需分三路：**自己合成 / 自己维护 / 借宿主** |

---

## 三层分流（本报告的核心模型）

| 路径 | 含义 | 典型对象 | 确定性 |
|---|---|---|---|
| **A · 借宿主** | 行为直接复用宿主内核 | `mmap` · `clock` · socket 数据面 · GPU | K1 对象存在 / K2 处理位置 |
| **B · 自己合成** | 返回值由用户态构造 | 身份(`uid`/`cap`) · `uname` · `/proc` 内容 | K1 结果 / K3 机制 |
| **C · 自己维护** | 状态由用户态持有 | 挂载树 · VFS · `/proc/net/*` · fd 视图 | K1 结果 / K2 机制 |

> **设计铁律**：能借的绝不自己实现（省性能）；必须"演"的绝不能漏（保正确性）。

---

## 结构图

```text
                Guest Android
                      │
          多个真实 Host carriers（进程/线程）
                      │
                      ▼
            ┌──────────────────┐
            │ Per-process shim │   ← 捕获 + 本地快速答复
            └────────┬─────────┘
                     │
              fast path（默认直通）
              /                    \
         Host 侧                 Broker 侧
      （真实资源）             （语义状态）
         mmap / VMA               vPID / uid / cap
         CFS 调度                 /proc 视图
         GPU / socket             mount tree
              \                    /
               └────────┬─────────┘
                        ▼
              一个统一的 Guest 世界
```

---

## 优点（推测）

| # | 优点 | 原因 |
|---|---|---|
| 1 | **数据面接近原生** | `mmap` / 页缓存 / 调度 / GPU / socket **全部借宿主**，不重写 |
| 2 | **不需要特权** | 纯 app 沙箱内实现，**不需要 root、不需要内核模块** |
| 3 | **兼容真实生态** | 跑的是**原版 Android 用户态**与 **Magisk 体系**，不是自制子集 |
| 4 | **对外是一台设备** | 身份、系统信息、挂载、`/proc` 全部投影成一台"不存在的真机" |
| 5 | **线程不需要模拟** | 客户机线程 = 宿主线程，**不需要用户态线程调度器** |
| 6 | **模块解耦** | 每个模块只做最少的事，职责清晰、可单独演进 |

---

## 代价与缺点（推测）

| # | 代价 / 缺点 | 原因 |
|---|---|---|
| 1 | **跨进程一致性最难** | 视图分散在多个进程，还要时刻保持一致（**主要工程量与风险**） |
| 2 | **投影维护成本高** | `/proc` 需逐字段自洽，且多接口不能互相矛盾（**产品自己也露馅**） |
| 3 | **版本脆弱** | 与 Magisk 等外部组件存在版本依赖，升级可能失效 |
| 4 | **性能有天花板** | 一旦拦截率升高，就逐步退化为"每个调用都陷入用户态" |
| 5 | **隔离依赖用户态语义** | 无 namespace、同 uid；隔离正确性由自己的实现负责 |
| 6 | **调试困难** | 逻辑全在用户态投影层，出错难定位 |
| 7 | **适配成本高** | 图形/外设需绑宿主 vendor HAL，**逐机型线性增长** |
| 8 | **"统一"是最贵的部分** | 统一的表象最便宜，维持统一最昂贵 |

---

## 它像什么、不像什么

| | 像 | 不像 |
|---|---|---|
| 对照 | **分布式垫片 + 中央协调** | 单进程 LibOS（gVisor）· namespace 容器（Docker） |
| 原因 | 多进程 + 全语义垫片 | 前者单地址空间；后者直通内核 |
| 一句话 | 从外部看，**"演"了一个内核** | 不是"造"了一个内核 |

---

## 阅读顺序建议

1. **本文（速览）** —— 一分钟理解思路与优劣
2. [`README.md`](README.md) §5 —— 架构模型与证据分层（K1/K2/K3）
3. [`垫片分层模型_进程与线程.md`](垫片分层模型_进程与线程.md) —— 进程/线程怎么套起来
4. [`假设验证_共享mm证伪.md`](假设验证_共享mm证伪.md) —— 方法论演示（假设→判据→证伪）
5. [`复现路线与垫片设计.md`](复现路线与垫片设计.md) —— 如果自己做，会怎么做
6. [`P1_归档_结构性观测.md`](P1_归档_结构性观测.md) —— 高权限层结构性观测

---

## 必须记住的边界

1. 本文与全仓库均为**未获授权的第三方分析**，**一切以官方为准**。
2. 所有架构结论分三层：**K1** 实测事实 · **K2/K3** 推断 / 不可区分 · **建议** 设计意见。
3. **不包含**任何可用于改写或提取的步骤、密钥或偏移表；本仓库**不含**产品源码与私有镜像。
4. 若官方明确表示禁止公开，我们将**手动关闭本仓库**。

---

*速览结束。本文为推测性总结，细节与证据请以 [`README.md`](README.md) 为准。*

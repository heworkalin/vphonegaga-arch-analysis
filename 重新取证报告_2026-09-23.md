# VPhoneGaGa 3.4.0 重新取证报告（双设备对照实验版）

> ## ⚠️ 重大更正（请先阅读）
>
> **本报告 §1.1 的“宿主 uid 2000 EACCES vs 虚拟机 uid 2000 ret=0”对照实验是无效的。**
>
> **原因**：宿主 uid 2000 调 `mount()` 得到 `EACCES`，是缺 `CAP_SYS_ADMIN` 的**必然结果**，
> 与投影层无关。该对照只能证明“两侧权限模型不同”，不能证明“虚拟机 `mount()` 不能由宿主内核执行”，
> 因为在这次对照中宿主内核**根本未被允许执行**。宿主也无 root、不允许 `unshare -Urm`/`-m`，
> 因此**无法构造宿主侧公平对照**。
>
> **正确的实验（已重跑）**：在**虚拟机内部**做 uid 扫描 —— 实测 uid `0 / 1000 / 2000 / 10000 / 10123`
> **均可成功 `mount(tmpfs)`**；而真实 Linux 不可能允许 uid 10000 挂载 `tmpfs`。
> 另：`mount` 对所有 uid 放行、`umount` 仅 uid 0 放行（**不对称**）。
>
> **结论不变**：“挂载是用户态虚拟实现”仍成立，但支撑它的是**虚拟机侧 uid 扫描、
> `/proc/filesystems` 白名单、宿主挂载表无变化、接口不一致、内存态**这五条有效证据，
> **而不是宿主 EACCES 对照**。详细重写见 [`README.md`](README.md) §4.6 与 §10.5 。
>
> **本文件保留原貌作为取证过程记录，结论以 [`README.md`](README.md) 为准。**

| 项目 | 内容 |
|---|---|
| 取证日期 | 2026-09-23 |
| 载体 | OnePlus PJE110（宿主，真实内核 5.15.167）+ 内置 Android 10 虚拟机实例 |
| **方法** | **双设备对照实验**：同一份自编译裸 syscall 测试程序，在宿主与虚拟机各运行一遍 |
| 宿主连接 | `emulator-5554` / `127.0.0.1:5555`（shell uid 2000） |
| 虚拟机连接 | `127.0.0.1:6556`（shell uid 2000 / su uid 0） |
| 分析对象 | `com.vphonegaga.titan` 3.4.0 |
| 事故 | 取证中一次 `bind mount /system` 破坏了虚拟机 /system 投影，重启实例已恢复（见 §7）|

---

## 0. 核心方法论：对照实验

旧分析的根本缺陷是「只在宿主侧静态观测，就断言虚拟机内部能力」。本报告采用**对照实验**：

> **同一份二进制、同一组系统调用、同一 uid，在"真实内核"与"投影层"上各跑一遍。差异即投影层的证据。**

自编译 `-nostdlib -static` 裸汇编 syscall 程序（无 libc、无动态链接），排除 libc 干扰。

---

## 1. 挂载子系统 —— 彻底推翻「无挂载能力」

### 1.1 对照实验（决定性的铁证）

目标路径 `/data/local/tmp/mtt`（双方都可创建），uid 均为 **2000（非 root）**：

| 系统调用 | **宿主机（真实 5.15 内核）** | **虚拟机（投影层）** |
|---|---|---|
| `mkdir(k)` | ret=0 | ret=0 |
| `mount("tmpfs", k, "tmpfs", 0)` | ❌ **ret=-13 (EACCES)** | ✅ **ret=0** |
| `mount("proc", k, "proc", 0)` | ❌ ret=-13 (EACCES) | ✅ ret=0 |
| `mount("/dev/null", k, "ext4", 0)` | ❌ ret=-13 | ❌ ret=-22 (EINVAL) |
| `mount("/system", k, "", MS_BIND)` | ❌ ret=-13 | ✅ ret=0 |
| `umount(k)` | ❌ ret=-1 (EPERM) | ❌ ret=-1 (EPERM) |
| `mkdir/"tmpfs"` 挂载点写入 | — | ❌ **EROFS（ro 生效）** |

**结论（K1，直接证明）**：
> 相同 uid、相同路径、相同二进制：**宿主内核拒绝（EACCES），虚拟机成功（ret=0）**。
> 若 `mount()` 只是转发宿主内核，结果必须与宿主一致。既然不一致，**虚拟机的 `mount()` 语义完全由用户态代码裁决**，与宿主内核无关。

### 1.2 为什么非 root 也能挂载？（虚拟权限模型）

真实 Linux 中 `mount()` 需要 `CAP_SYS_ADMIN`；宿主 uid 2000 得到 EACCES。
虚拟机 uid 2000 得到 ret=0 ⇒ **权限判定基于「虚拟 uid」，而非真实 uid**。

**不对称性**：`mount` 允许非 root，但 `umount` 返回 EPERM（root 才 ret=0）——说明用户态实现有独立且不完全对称的权限表。

### 1.3 能力边界 = `/proc/filesystems` 白名单

| 虚拟机 `/proc/filesystems` | 挂载结果 |
|---|---|
| rootfs | ✅ ret=0 |
| proc | ✅ ret=0 |
| tmpfs | ✅ ret=0 |
| devpts | ✅ ret=0 |
| selinuxfs | ✅ ret=0 |
| socketfs | ❌ ret=1 |
| sdcardfs | ❌ ret=1 |
| **任何不在列表中的**（ext2/3/4/f2fs/btrfs/overlay/squashfs/cgroup2/ramfs/debugfs/tracefs/securityfs/binfmt_misc） | ❌ **EINVAL(22)** |

宿主 `/proc/filesystems` 有 30+ 项（含 ext4/f2fs/erofs/overlay/ramfs…）；虚拟机只有 7 项。
⇒ **`mount` 实现对着这张硬编码白名单做校验，不在表内直接 EINVAL。**

### 1.4 语义精细度（部分实现）

| 特性 | 行为 |
|---|---|
| `-o ro` | ✅ **真正强制**（写入报 EROFS）|
| `-o remount,rw` | ✅ 生效 |
| `-o noexec,nosuid,nodev` | ✅ 记录并生效 |
| `-o mode=0755,uid=1000,gid=1000` | ⚠️ **只写进 /proc/mounts 文本，实际目录仍 777 root:root**（不实施）|
| 挂载点不存在 | ❌ ENOENT |
| `--move` | ❌ EINVAL（未实现）|
| `--bind` | ✅ ret=0（但对已叠加目标有危险，见 §7）|
| `df` 对虚拟挂载 | **不可见**（无 statfs 应答）|

### 1.5 宿主侧完全不可见（K1）

- 虚拟机 `mount` 后，**宿主 `/proc/mounts` 无任何新增**
- 宿主挂载表 194 条；虚拟机挂载表 49 条，**无一条重合的产品专属挂载点**
- 宿主侧 grep `vphonegaga|instance1|readonly` = 空

### 1.6 结论

> **旧分析「宿主挂载表看不到 → 没有挂载能力」是无效黑盒推论。**
> 实测：虚拟环境**具备部分挂载语义**，维护一棵**独立于宿主内核 VFS 的用户态挂载树**；能力有白名单边界，`mode/uid/gid` 等参数不实施。宿主内核全程无感知。

---

## 2. 网络子系统 —— 超越「路径 A / 路径 B」二分

### 2.1 socket 是真实的宿主 socket（K1）

在虚拟机内建监听 socket（端口 4660 / 0x1234），随即在宿主侧查询：

```
宿主 /proc/net/tcp:
   5: 00000000:1234 00000000:0000 0A ... uid=10383 inode=11636144   ← 真实存在！
虚拟机内部 /proc/net/tcp:  (空，被投影隐藏)
```

⇒ **虚拟机的 socket 是宿主内核的真实 socket**（uid 显示为 app 真实 uid 10383）。
⇒ 网络命名空间**与宿主共享**（宿主读虚拟内核进程的 `/proc/7674/net/tcp` = 186 条 ≈ 宿主全局 183 条）。

### 2.2 但 setsockopt 被用户态层拦截（对照实验，K1）

同一二进制在双方运行：

| socket 选项 | 宿主（真 5.15） | 虚拟机（投影） |
|---|---|---|
| IP_TOS(1) / IP_TTL(2) / IP_MTU_DISCOVER(10) | ✅ | ✅ 一致 |
| **IP_RETOPTS(7)** | ✅ 0 | ❌ **EINVAL** |
| **IP_PKTINFO(8)** | ✅ 0 | ❌ **EINVAL** |
| **IP_RECVTTL(12)** | ✅ 0 | ❌ **EINVAL** |
| SO_REUSEADDR / SO_KEEPALIVE / SO_RCVBUF | ✅ | ✅ 一致 |
| TCP_NODELAY / TCP_KEEPIDLE | ✅ | ✅ 一致 |
| **UDP 的 IP_RECVTTL / IP_PKTINFO** | ✅ 0 | ❌ **EINVAL** |

⇒ 宿主内核接受这些选项，虚拟机拒绝 ⇒ **`setsockopt` 被用户态层拦截，只放行白名单子集。**

### 2.3 现象印证

- 虚拟机内 `ping`：`setsockopt(IP_RECVTTL): Invalid argument`，且回复 **`ttl=0`**（异常，因为 TTL 取值路径被拦截）
- 虚拟机内部 `/proc/net/tcp`、`/proc/net/tcp6` **为空**；`/proc/net/sockstat` **不存在** ⇒ 网络 procfs 被投影
- 虚拟机 `/proc/net/unix` 找**不到** `@titan-pipe-*`（宿主侧可见 38 个）

### 2.4 结论

> 网络**既不是纯路径 A，也不是纯路径 B**，而是**混合模型**：
> - **数据面**：socket 是真实宿主 socket，TCP/IP 协议栈由**宿主内核**处理（路径 A 成分）
> - **控制面**：`setsockopt` 等被**用户态代理层**拦截并做白名单过滤（路径 B 式拦截）
> - **信息面**：`/proc/net/*` 全被**投影/过滤**，隐藏真实连接
>
> 旧分析「能联网 ⇒ 复用宿主协议栈」同样越界；正确表述应为「观测到宿主内核确实建立了真实 socket，但 socket 控制面存在用户态拦截层」。

---

## 3. 身份与系统信息投影（对照实验）

### 3.1 裸 syscall 对照

| 系统调用 | 宿主（真 5.15） | 虚拟机（投影） | 判定 |
|---|---|---|---|
| `uname().release` | `5.15.167-android13-8-…` | **`4.14.42-titan`** | 投影 |
| `uname().version` | `#1 SMP …2025-09-03` | **`#34 …2019-11-09`** | 投影 |
| `sysinfo().totalram` | 15,868,604,416 (14.8G) | **4,160,749,568 (3.9G)** | 投影 |
| `sysinfo().procs` | 12982 | **851** | 投影 |
| `clock_gettime(REALTIME)` | 1790172229 | **1790172229** | **直通（共享）** |
| `getuid/geteuid` | 2000 | 2000 | 一致 |
| `sched_getaffinity` 返回字节数 | 8 | **128** | 实现差异 |

### 3.2 宿主侧 /proc 真相 vs 虚拟机自述

| 项目 | 宿主内核真相 | 虚拟机自述 |
|---|---|---|
| uid | **10383** | 0 (root) |
| CapEff | **0** | 全 0x3fffffffff |
| NoNewPrivs | **1** | 0 |
| Seccomp | **2**（2 层） | 0 |
| TracerPid | **0** | 0 |
| 内核 | 5.15.167 | 4.14.42-titan |
| CPU | 0xd46 (Cortex-A510) | 0x801 (Cortex-A53) |
| 内存 | 14.8 GB | 3.9 GB |

### 3.3 procfs 投影破绽（用产品自身 bug 证明投影）

| 破绽 | 证据 |
|---|---|
| `sock:[782]` | 真内核恒为 `socket:[inode]` |
| `/proc/1/exe` 权限 `lr--r--r--` | 真 procfs 恒为 `lrwxrwxrwx` |
| `/proc/uptime` = 575,125,793 秒（≈18.2 年） | 结构体拼装错误，且 idle > uptime |
| `mount` 与 `/proc/mounts` 设备名不一致 | `mtdblock0` vs `platform/host/by-name/system` |
| `/proc/net/dev` 的 lo/eth0/wlan0 字节数完全相同 | 三接口复用了同一份数据 |
| `/share` 挂载源泄漏宿主路径 | `/storage/emulated/0/Android/data/com.vphonegaga.titan/...` |
| `/data`、`/cache` 显示 933 GB | 未伪装，泄漏宿主真实容量 |
| SELinux 上下文 `--  u:object_r:...` | 多余 `--`，仿真瑕疵 |

---

## 4. Seccomp 分层（宿主侧，K1）

| 进程 | Seccomp | **Seccomp_filters** | CapEff |
|---|---|---|---|
| 宿主 zygote64 (1450) | 0 | 0 | full |
| `com.vphonegaga.titan` (6346) | 2 | **1** | 0 |
| `:instance1` (7476) | 2 | **1** | 0 |
| **`titan64_0:kernel` (7674)** | 2 | **2** ← 跃迁 | 0 |
| `titan32_0:kernel` (7734) | 2 | **2** | 0 |
| 客户机 init (7750) | 2 | **2** | 0 |
| magiskd (8011) | 2 | **2** | 0 |
| 全部客户机进程 | 2 | **2** | 0 |

⇒ **第 2 层 seccomp 过滤器由虚拟内核在自举时安装，全客户机树继承。** 排除 ptrace（TracerPid 全 0）。

---

## 5. 架构模型（推演，K2/K3）

### 5.1 进程树（宿主侧真实 PPid）

```
宿主 zygote64 (1450)
├── com.vphonegaga.titan (6346)
└── com.vphonegaga.titan:instance1 (7476)     ← 二者是兄弟
    └── titan64_0:kernel (7674)  Name=libloader64.so
        ├── titan32_0:kernel (7734)           ← 由 64 位 fork
        ├── titan64_1:init (7750)             ← init 真父 = 虚拟内核
        ├── titan64_43:magiskd (8011)         ← **真父 = 虚拟内核，不是 init**
        ├── titan64_56:lspd (8083)            ← 真父 = 虚拟内核
        ├── titan64_229:zygiskd64 (8577)      ← 真父 = 虚拟内核
        └── titan32_504:zygiskd32 (9625)      ← 真父 = 32 位虚拟内核
```

- 全部客户机进程宿主侧 uid = **10383**、CapEff = **0**、NoNewPrivs = **1**
- 客户机侧 PPid 被**重建**成真机形态（`init(1) → magiskd(43) → {lspd, zygiskd}`）

### 5.2 IPC 通道（宿主侧可见，虚拟机内不可见）

```
@titan-pipe-1-{ipc,camera,sensors,network,input-qwerty,input-gpio-keys,
               gsm,gps,framebuffer,fingerprint,activity,hw-control,crash}
@titan-process-worker-server-1-<宿主PID>   × 95
@titan-1-process-monitor
```

### 5.3 投影机制模型（K2，可解释全部现象）

> 用户态运行时拦截系统调用 → 依业务类型分流：
> **合成**（uname/sysinfo/身份/`/proc` 内容）、**重定向**（路径改写）、**透传**（clock/mmap/futex）；
> 挂载维护独立的用户态挂载树；网络维护独立的 socket 控制面白名单，数据面走宿主真实 socket。

### 5.4 未确认项（K3）

1. IPC 消息二进制协议、编码、同步/异步策略
2. 哪些调用走完整 IPC、哪些本地就地处理
3. 网络数据面是否有额外用户态处理（当前证据支持宿主内核处理数据面）
4. 挂载树内部数据结构
5. seccomp 具体策略（需读 BPF 程序）

---

## 6. 与旧结论的对比表

| # | 旧结论 | 本次判定 | 依据 |
|---|---|---|---|
| 1 | 「宿主挂载表无记录 ⇒ 无挂载能力」 | **推翻** | 对照实验：宿主 EACCES vs 虚拟机 ret=0 |
| 2 | 「能联网 ⇒ 复用宿主协议栈」 | **修正** | 混合模型：真实宿主 socket + 用户态 setsockopt 拦截 |
| 3 | 「简单库劫持，就地处理」 | **修正** | 跨组件 IPC + 用户态 VFS + 投影 |
| 4 | 「root 是模拟状态机」 | **修正** | 实为 Magisk 26.0 体系，靠 uid/cap 投影工作 |
| 5 | 「宿主看不见 ⇒ 功能不存在」 | **推翻** | 范式错误：用户态自建语义，宿主本就不可见 |

---

## 7. 取证事故记录（如实披露）

**事故**：`bind mount /system` 到「已叠加挂载的目标」后，虚拟机 `/system` 投影失效（`/system/bin/sh` 消失，`adb shell` 无法启动）。

**原因**：虚拟机的用户态挂载实现对「向已挂载目标做 bind mount」处理有缺陷，覆盖/解绑了源 `/system` 的 VFS 映射。

**恢复**：重启虚拟机实例（容器文件在磁盘未动）→ 已完全恢复，`/system/bin/sh` (299616B)、`toybox` 正常。

**后续处置**：改用 `/data/local/tmp` 作为挂载目标，每个测试后立即 `umount`，不再对系统路径做 bind mount。

---

## 8. 可复现命令

```bash
# 双设备
adb connect 127.0.0.1:5555    # 宿主
adb connect 127.0.0.1:6556    # 虚拟机

# 对照运行（同一二进制）
adb push raw5 /data/local/tmp/ && adb -s emulator-5554 shell /data/local/tmp/raw5
adb -s 127.0.0.1:6556 shell /data/local/tmp/raw5

# 挂载能力
adb -s 127.0.0.1:6556 shell "su -c 'mount -t tmpfs tmpfs /data/local/tmp/mm; echo \$?; umount /data/local/tmp/mm'"

# 对照 socket 选项 / 系统信息
adb push raw4 raw6 /data/local/tmp/
```

---

*报告结束。全部结论基于 2026-09-23 实机双设备对照取证，命令可复现。*
